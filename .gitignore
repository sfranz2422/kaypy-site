# site/ is NOT here on purpose, and must never be. It is not build output —
# it IS the website, hand-written, and Render serves it with an empty build
# command. Ignoring it would deploy an empty site. See README.md, "Deploying".

.DS_Store
__pycache__/
*.pyc

# The proof sheet make_logo.py writes with --proof. Something to look at once,
# not something to keep.
logo-proof.png
